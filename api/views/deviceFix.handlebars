<h1>Fix {{ fix.id }} for Device {{ device.id }}</h1>
<div>
  {{#if errorMessage}}
    {{errorMessage}}
  {{else}}
    Are you sure? {{fix.description}}
    <form method="POST"><input type="submit"/></form>
  {{/if}}
</div>
